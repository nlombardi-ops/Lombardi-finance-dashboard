# Login
DASHBOARD_PASSWORD=
DASHBOARD_TOKEN=

# Optional: persist writes to Vercel Blob instead of local files in production.
# Auto-created when you attach a Vercel Blob store to the project (Storage tab).
BLOB_READ_WRITE_TOKEN=

# Optional: Finance Q&A chat
ANTHROPIC_API_KEY=

# Optional: Google Drive auto-sync for bollette (and any other area you wire up)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REFRESH_TOKEN=
CRON_SECRET=
